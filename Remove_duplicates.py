def remove_duplicates(arr):
    arr1 = list(set(arr))
    arr1.sort()
    print(arr1)


def remove_duplicate_unsorted(num):
    arraylist = list(set(arr2))
    arraylist.sort()
    print(arraylist)

arr2 = [2,3,1,9,3,1,3,9]
m = len(arr2)
arr = [1,1,2,2,2,3,3]
n = len(arr)
remove_duplicates(arr)
remove_duplicate_unsorted(arr2)


#  a list is a collection of items that are ordered and mutable (modifiable).
# Lists are created by placing items inside square brackets [].

# a set is an unordered collection of unique elements. Hence, here we use set for unique values.
# Sets are created by placing comma-separated values inside curly braces {}.